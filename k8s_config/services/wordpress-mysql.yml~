apiVersion: v1
kind: Service
metadata:
        name: wordpress-mysql
spec:
        ports:
                - port: 31002
                  nodePort: 31002
                  targetPort: mysql-port
                  protocol: TCP
        selector:
                app: mysql-wordpress
        type: NodePort

